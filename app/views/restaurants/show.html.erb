<h1>Welcome to</h1>
<p><%= "#{@restaurant.name} - #{@restaurant.address} - #{@restaurant.phone_number}" %></p>
<%= render "review", reviews: @restaurant.reviews %>
<%= link_to "Restaurants", restaurants_path, class: "btn btn-primary" %>
<%= link_to "Leave a review",  new_restaurant_review_path(@restaurant), class: "btn btn-primary" %>
<%= link_to "Edit",  edit_restaurant_path(@restaurant), class: "btn btn-primary" %>
<%= link_to restaurant_path(@restaurant), class: "btn btn-primary",
      data: {turbo_method: :delete, turbo_confirm: "Delete?"} do %>
      Delete
<% end %>



